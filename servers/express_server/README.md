# Express server

REST API with in-memory todos and users.

## Routes

- `GET /health`
- `GET /todos`, `POST /todos`, `PATCH /todos/:id`
- `GET /users`

## Environment

- `PORT` — default **3000**

## Scripts

- `pnpm dev` — `tsx watch`
- `pnpm build` / `pnpm start` — compiled `dist/`
