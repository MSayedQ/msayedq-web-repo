# Nuxt 3

SSR pages with a todo screen; uses `runtimeConfig.public.apiUrl`.

## Environment

- `NUXT_PUBLIC_API_URL` (default `http://localhost:3000`)

## Scripts

- `pnpm dev` — port **3003**
- `pnpm build` — Nitro output in `.output/`
