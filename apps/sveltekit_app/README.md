# SvelteKit

SSR-capable app with a todo page calling Express.

## Environment

- `PUBLIC_API_URL` — SvelteKit public env prefix (default `http://localhost:3000`)

## Scripts

- `pnpm dev` — port **5177**
- `pnpm build`
