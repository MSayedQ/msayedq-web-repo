# @repo/config

Centralized **Prettier**, **ESLint** (flat config), and **TypeScript** base JSON files.

Import paths:

- `@repo/config/eslint`
- `@repo/config/prettier`
- `@repo/config/typescript/base` (and `node`, `react`)
